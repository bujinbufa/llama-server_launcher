<Window x:Class="LlamaManager.Views.EditConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:LlamaManager.Views.Controls"
        Title="编辑模型配置"
        Width="1100"
        Height="700"
        WindowStartupLocation="CenterOwner"
        FontSize="14">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <controls:ConfigForm x:Name="ConfigForm"/>
        <Button Grid.Row="1" Content="保存修改" Height="35" Margin="0,10,0,0" Click="SaveButton_Click"/>
    </Grid>
</Window>
